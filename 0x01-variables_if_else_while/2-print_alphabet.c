#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	/* putchar - write c to stout */

	char c;

	for (c = 'a'; c <= 'z'; c++)

	putchar(c);
	putchar('\n');

	return (0);
}
